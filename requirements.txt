# =============================================================================
# Core Dependencies - Required for basic functionality
# =============================================================================

# News aggregation
feedparser>=6.0.11
beautifulsoup4>=4.12.3
requests>=2.31.0
python-dateutil>=2.8.2

# Data processing
pandas>=2.2.1
numpy>=1.26.0,<2.0.0  # Pin to 1.x for spaCy/thinc compatibility on Streamlit Cloud

# Environment variables
python-dotenv>=1.0.0

# =============================================================================
# AI and NLP Dependencies
# =============================================================================

# OpenAI-compatible SDK used by src/llm_client.py to talk to Cerebras,
# Groq, and OpenRouter (all expose the same chat-completions interface).
openai>=1.40.0

# scikit-learn for topic clustering and TF-IDF
scikit-learn>=1.3.2

# spaCy for NLP (noun chunks, named entities)
spacy>=3.7.2
# spaCy English model - direct URL install for Streamlit Cloud
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl

# =============================================================================
# Dashboard and Visualization
# =============================================================================

# Streamlit dashboard
streamlit>=1.32.0

# Plotly for interactive charts
plotly>=5.20.0

# =============================================================================
# Configuration (Optional - fallback to defaults if not installed)
# =============================================================================

# YAML config file support
pyyaml>=6.0.1

# =============================================================================
# Scheduling
# =============================================================================

# schedule for Python-based cron-like scheduling
schedule>=1.2.0

# =============================================================================
# Testing
# =============================================================================

# pytest for unit tests
pytest>=8.0.0

# pytest-anyio for async testing support
anyio>=4.3.0

# time-machine for datetime mocking in tests
time-machine>=2.14.0
